# Prototypes

**Not production code.** Experiments and design references to port into `src/` when ready.

| Folder | Stack | Run |
|--------|-------|-----|
| `next-ui-lab/` | Next.js 15 | `npm run prototype:web` from repo root |

When a prototype pattern ships in `src/`, update or remove the prototype here to avoid drift.

/** Days a hit stays in the Vault before it auto-converts to coins if not shipped or traded. */
export const VAULT_HOLD_DAYS = 14;

/** Show “expiring soon” styling and copy when time left is under this threshold. */
export const VAULT_EXPIRY_WARN_MS = 24 * 60 * 60 * 1000;

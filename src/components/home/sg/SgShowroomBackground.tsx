import React from 'react';
import { TerminalBackdrop } from '../../terminal/TerminalBackdrop';

/**
 * Screen ground for the Tokyo Night Terminal chassis.
 */
export function SgShowroomBackground() {
  return <TerminalBackdrop />;
}

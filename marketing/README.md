# Marketing

Static assets outside the Expo app.

| Path | Description |
|------|-------------|
| `landing/index.html` | Standalone marketing page — open with `npm run marketing:landing` |

The live product is `src/` (Expo), not this folder.

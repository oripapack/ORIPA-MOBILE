# Pack opening SFX

Short PCM `.wav` files used by `expo-av` in `src/audio/packOpeningFeedback.ts`.

Regenerate (e.g. after tweaking the generator):

```bash
npm run generate:sfx
```

Source script: `scripts/generate-pack-sfx.mjs`

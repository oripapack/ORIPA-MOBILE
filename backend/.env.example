# Copy to backend/.env for local Supabase CLI (optional until Clerk auth is wired).
# Find your domain: Clerk Dashboard → Supabase integration, or *.clerk.accounts.dev

CLERK_DOMAIN=your-instance.clerk.accounts.dev

{
  "name": "pull-hub-backend",
  "private": true,
  "scripts": {
    "start": "npx supabase start",
    "stop": "npx supabase stop",
    "status": "npx supabase status",
    "db:reset": "npx supabase db reset",
    "db:push": "npx supabase db push",
    "link": "npx supabase link",
    "functions:deploy": "npx supabase functions deploy execute-pull && npx supabase functions deploy request-shipment && npx supabase functions deploy stripe-webhook && npx supabase functions deploy mint-retry",
    "secrets:setup": "echo 'Execute manual script to populate production secrets for Stripe, Base minting, and Cron'"
  }
}

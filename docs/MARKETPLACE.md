# Marketplace (Shop tab)

- **Listings only** — singles / sealed style cards with price; **no pack-opening flow** (that stays on **Home**).
- Mock data: `src/data/marketplace.ts` (`marketplaceStores`, `marketplaceListings`).
- UI: `MarketplaceScreen`, `ListingCard`, `WhyChoosePullHub`.
- Tapping a listing shows a demo alert until checkout is implemented.
